# Official fnpack tool, fetched by scripts/fetch-fnpack.sh
/fnpack

# Build workspace and outputs
/build/
/dist/

# Assembled application payload inside the fnpack projects.
# They are rebuilt from upstream artifacts plus the src/<app>/app overlays by
# scripts/build-<app>.sh and must not be committed.
/embyserver/app/
/qbittorrent/app/

*.deb
*.fpk
